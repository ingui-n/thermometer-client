#ifndef ENV
#define ENV
#define DEVICE_NAME ""
#define RECEIVE_MAC {0xFF, 0xFF, 0xFF, 0xFF, 0xFF, 0xFF} // MAC address of the receive controller
#define SERIAL_BAUDRATE 9600
#define THERMOMETER_PIN 13 // GPIO pin
#define TEMPERATURE_CALIBRATION (0.0) // value to add to the measured temperature like (-0.5) or (0.2)
#define ESP_NOW_PASSWORD "" // same as on controller
#define ESP_NOW_CHANNEL 1 // wifi channel
#endif // ENV
